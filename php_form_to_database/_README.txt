This example shows how to structure PHP files and code.

The init.php file contains things that all files in the APP will need.

At the top of each file that generates a Web page (files that begin with person_), the init.php file is required.

